colors:
  body: none
  date: black
  tags: yellow
  title: cyan
default_hour: 9
default_minute: 0
editor: /usr/bin/nvim
encrypt: false
highlight: true
indent_character: '|'
journals:
  default:
    # TODO: Might want to change the location,
    # put it somewhere we can include it in
    # version control...
    # Maybe: ~/Documents/notes with my so-called
    # "notebook".
    journal: /home/thombruce/Documents/notes/journal.txt
linewrap: 79
tagsymbols: '#@'
template: false
timeformat: '%F %r'
version: v4.2.1
